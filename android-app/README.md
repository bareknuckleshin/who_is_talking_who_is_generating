# Android App (미구현)

현재 Android 앱은 아직 구현하지 않았습니다.

향후 구현 권장 방향:

1. **빠른 MVP**: WebView 기반 래퍼 앱으로 `web-frontend`를 로드
2. **장기 방향**: 네이티브 UI + WebSocket 직접 연동으로 전환

초기 디렉터리 구조 예시:

```
android-app/
  app/
  build.gradle.kts
  settings.gradle.kts
```
